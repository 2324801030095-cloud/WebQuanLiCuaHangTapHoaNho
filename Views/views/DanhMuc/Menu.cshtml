@model IEnumerable<WebQuanLiCuaHangTapHoa.Models.DanhMuc>

@{
    // 🔤 Bản đồ tên không dấu -> có dấu (tạm thời cho UI)
    //   Khi rảnh có thể cập nhật TenDM trong DB cho “đúng chuẩn” và bỏ map này.
    var mapTenCoDau = new System.Collections.Generic.Dictionary<string, string>
    {
        ["BanhKeo"] = "Bánh kẹo",
        ["BoVaTrung"] = "Bơ & Trứng",
        ["DoUong"] = "Đồ uống",
        ["GaoVaNguCoc"] = "Gạo & Ngũ cốc",
        ["GiaViVaDauAn"] = "Gia vị & Dầu ăn",
        ["HoaPhamGiaDung"] = "Hóa phẩm gia dụng",
        ["RauCuQua"] = "Rau củ quả",
        ["SuaCacLoai"] = "Sữa các loại"
    };
}

@if (Model == null || !Model.Any())
{
    <!-- Không có danh mục -->
    <span class="text-muted small">Chưa có danh mục</span>
}
else
{
    foreach (var dm in Model)
    {
        // 🏷️ Lấy tên có dấu nếu có trong map, không thì dùng tên gốc
        var tenHienThi = mapTenCoDau.ContainsKey(dm.TenDM) ? mapTenCoDau[dm.TenDM] : dm.TenDM;

        <a class="dropdown-item d-flex align-items-center"
           href="@Url.Action("DanhSach","DanhMuc", new { maDM = dm.MaDM })"
           title="@dm.MoTa">
            <i class="bi bi-tag text-success me-2"></i>
            <span>@tenHienThi</span>
        </a>
    }
}
